import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  textingGif: {
    width: 350,
    height: 350,
    borderRadius: 50,
    marginTop: 50,
  },
});

export default styles;
